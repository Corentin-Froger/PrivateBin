name: GitHub Actions Demo
run-name: ${{ github.actor }} vient de push, tests en cours
on: [push]
jobs:
  Install:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: php-actions/composer@v6 # or alternative dependency management
      - run: sudo make install
      - run: sudo bin/composer update
      - run: sudo apt install php-gd php-sqlite3 php-xdebug
  Tests:
    needs: Install
    runs-on: ubuntu-latest
    steps:
      #- uses: php-actions/phpunit@v3
      - run: XDEBUG_MODE=coverage ./vendor/bin/phpunit –coverage-text=coverage_test.txt
      #- run: make test
